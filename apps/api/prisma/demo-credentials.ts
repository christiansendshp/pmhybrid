/**
 * Local-dev-only seed credential — not a real secret, just a documented
 * demo login (README.md). Shared between seed.ts and the auth e2e test so
 * they can never drift apart.
 */
export const DEMO_EMAIL = 'demo-human@pmhybrid.local';
export const DEMO_PASSWORD = 'demo1234';
