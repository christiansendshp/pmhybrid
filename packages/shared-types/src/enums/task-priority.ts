/** Task priority (brief §6 "prioridad") — a closed set, so boards can filter and sort by it. */
export enum TaskPriority {
  LOW = 'LOW',
  MEDIUM = 'MEDIUM',
  HIGH = 'HIGH',
  CRITICAL = 'CRITICAL',
}
